#include <stdio.h>

/* Basic queue data structure
 * Supports dequeuing and enqueuing values
 */

struct Queue {
	// Implement Queue logic
};

void enqueue(struct Queue *queue, int value) {
	// Add item to queue
}

void dequeue(struct Queue *queue) {
	// Remove item from queue
}

void peek(struct Queue *queue) {
	// This could print or return
}

int rear(struct Queue *queue) {
	// Returns back of queue without removing
}

int is_full(struct Queue *queue) {
	// Return 0 if not empty 1 if it is
}

int is_null(struct Queue *queue) {
	// Return 0 if not empty (null?)
}

/* Implement queue struct */
int main() {
	return 0;
}
