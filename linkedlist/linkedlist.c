#include <stdio.h>
#include <stdlib.h>

struct node;

struct list {
	struct node *head;
};

struct node {
	int data;
	struct node *next;
};

/* Implement linked list struct */
int main() {
	printf("Test");
	return 0;
}
